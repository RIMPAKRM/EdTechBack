services:
  - type: web
    name: edtech-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.backend.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.14
